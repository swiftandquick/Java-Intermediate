package s16p183_Lambda2;

public interface Lambda {
	
	/* No parameter arguments, but notice how the method type is int, so 
	 * that means the overridden method must return an int value.  */
	abstract public int demo();
	
}
